// -*- tab-width: 2; indent-tabs-mode: nil; coding: utf-8-with-signature -*-
//-----------------------------------------------------------------------------
// Copyright 2000-2025 CEA (www.cea.fr) IFPEN (www.ifpenergiesnouvelles.com)
// See the top-level COPYRIGHT file for details.
// SPDX-License-Identifier: Apache-2.0
//-----------------------------------------------------------------------------
#include "ContactGroup.h"
/* Author : havep at Tue Jul  9 18:28:48 2013
 * Generated by createNew
 */

/*---------------------------------------------------------------------------*/

#include <arcane/utils/FatalErrorException.h>
#include "ArcGeoSim/Mesh/SurfaceUtils/ContactGroupImpl.h"
#include "ArcGeoSim/Mesh/SurfaceUtils/IContactFamily.h"

/*---------------------------------------------------------------------------*/

ContactGroup::
ContactGroup() 
  : m_impl(NULL)
{ 
  ;
}

/*---------------------------------------------------------------------------*/

ContactGroup::
ContactGroup(const ContactGroup & rhs) 
  : m_impl(rhs.m_impl)
{
  ;
}

/*---------------------------------------------------------------------------*/

ContactGroup::
ContactGroup(ContactGroupImpl * group) 
  : m_impl(group)
{ 
  ;
}

/*---------------------------------------------------------------------------*/

ContactGroup::
~ContactGroup() 
{ 
  ;
}

/*---------------------------------------------------------------------------*/

ContactGroup & 
ContactGroup::
operator=(const ContactGroup & rhs)
{
  m_impl = rhs.m_impl;
  return *this;
}

/*---------------------------------------------------------------------------*/

ContactEnumerator 
ContactGroup::
enumerator() const
{ 
  if (m_impl)
    return m_impl->enumerator();
  else
    return ContactEnumerator();
}

/*---------------------------------------------------------------------------*/

void 
ContactGroup::
setItems(const Int32ConstArrayView & lids)
{
  if (m_impl)
    return m_impl->setItems(lids);
  else
    throw Arcane::FatalErrorException(A_FUNCINFO,"Cannot set contacts on a null group");
}

/*---------------------------------------------------------------------------*/

void 
ContactGroup::
addItems(const Int32ConstArrayView & lids)
{
  if (m_impl)
    return m_impl->addItems(lids);
  else
    throw Arcane::FatalErrorException(A_FUNCINFO,"Cannot set contacts on a null group");
}

/*---------------------------------------------------------------------------*/

Integer 
ContactGroup::
size() const 
{ 
  if (m_impl)
    return m_impl->size();
  else
    return 0;
}

/*---------------------------------------------------------------------------*/

bool 
ContactGroup::
empty() const 
{
  if (m_impl)
    return m_impl->empty();
  else
    return true;
}

/*---------------------------------------------------------------------------*/
  
bool 
ContactGroup::
null() const 
{ 
  return m_impl == NULL;
}

/*---------------------------------------------------------------------------*/

String 
ContactGroup::
name() const {
  if (m_impl)
    return m_impl->name();
  else
    return String();
}

/*---------------------------------------------------------------------------*/

IContactFamily * 
ContactGroup::
family() const
{
  if (m_impl) 
    return m_impl->family();
  else 
    return NULL;
}

/*---------------------------------------------------------------------------*/

ContactGroupImpl * 
ContactGroup::
impl() const
{
  return m_impl;
}

/*---------------------------------------------------------------------------*/
/*---------------------------------------------------------------------------*/
