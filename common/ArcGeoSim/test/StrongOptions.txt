# -*- cmake -*-
if(TARGET strong_options AND NOT WIN32)
ARCANE_ADD_TEST_SEQUENTIAL(ComplexStrongOptions
  ArcGeoSim
  ${ARCGEOSIM_TEST_DIR}/ArcGeoSim/test/ServiceTests/StrongOptions
  ComplexStrongOptions.arc)
endif()
